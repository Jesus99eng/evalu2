

  function FiltrarGohan(array) {
    return array.filter((item) => item.name === "Gohan");
  }



function filtrarArrayMayorPoder(array) {
    return array.filter((item) => item.price >= 50);
  }

  function filtrarArrayMenorPoder(array) {
    return array.filter((item) => item.price < 50);
  }

